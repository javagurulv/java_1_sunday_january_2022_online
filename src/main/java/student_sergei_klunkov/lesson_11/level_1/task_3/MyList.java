package student_sergei_klunkov.lesson_11.level_1.task_3;

interface MyList {

    void addObjectInList(Object object);

    // Добавить объект в List возможно, а в Array нельзя, потому, что длинна массива уже зафиксирована.

    void removeObjectFromTheList(Object object);

    // Удалить объект из List после создания возмодно, из Array нельзя, длина массива зафиксирована.


}
