1. Top class is useless to declare as private, because you cannot access it, eventually compiler
will give an error "We can not access private class".
2. Only in case of nested classes, inner class can be declared as private.